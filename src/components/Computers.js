import React, { Component } from 'react';
import Gallery from './Gallery';

class Computers extends Component {
    render() {
        return (<Gallery query='computers' />);
    }
}

export default Computers;